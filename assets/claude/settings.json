{
  "permissions": {
    "additionalDirectories": [
      "{{STARTUP_PATH}}"
    ]
  },
  "statusLine": {
    "type": "command",
    "command": "{{STARTUP_PATH}}/bin/statusline{{SHELL_SCRIPT_EXTENSION}}"
  }
}
