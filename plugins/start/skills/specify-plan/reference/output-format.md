# Output Format Reference

Guidelines for PLAN output. See `examples/output-example.md` for a concrete rendered example.

---

## Next-Step Options

Use `AskUserQuestion` based on plan state:

**During phase definition:**
- "Define next phase"
- "Run validation"
- "Address gaps in current phase"
- "Complete PLAN"

**After validation:**
- "Fix validation failures"
- "Re-run validation"
- "Proceed to implementation"
